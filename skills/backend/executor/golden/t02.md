# t02 — basic CRUD endpoint

## User intent (verbatim)
Add POST /items and GET /items/{id} endpoints with an in-memory store. Include pytest tests using FastAPI TestClient.

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- POST /items accepts JSON body and returns 201 with created item (including generated id)
- GET /items/{id} returns 200 with the item JSON
- GET /items/{id} returns 404 for non-existent id
- In-memory store (dict) scoped at module level
- Pytest tests exist using TestClient; `pytest -q` passes

## Constraints (clauses)
- k1: must include tests for both endpoints
- k2: must use type hints
- k3: item must auto-generate an id on creation

## Anti-patterns (what counts as failure)
- using a database instead of in-memory store
- missing 404 case for missing item
- no test for the POST endpoint
- skipping the receipt marker

## Expected complexity
trivial

## Known trace
trace_id: 0c46e24b-1420-4de6-b23a-5a2844f7f1b1
manual_label: fail
failure_mode: incomplete_implementation
observations: 15
duration_s: 26

## Notes
OpenCode started reading project files but was terminated before writing any code.
Only 5 messages, 22 parts — never progressed past exploration phase.
Receipt was not emitted. Task needs to be re-spawned for completion.
