# t07 — CRUD items (PUT + DELETE)

## User intent (verbatim)
Add PUT /items/{id} and DELETE /items/{id} endpoints. PUT updates item fields, returns 200 or 404. DELETE removes item, returns 204 or 404. Include pytest tests.

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- PUT /items/{id} accepts partial JSON body (name and/or price), returns 200 with updated item
- PUT /items/{id} returns 404 for non-existent id
- DELETE /items/{id} removes item and returns 204
- DELETE /items/{id} returns 404 for non-existent id
- pytest tests exist using TestClient; `pytest -q` passes

## Constraints (clauses)
- k1: must include tests for both endpoints (happy + 404)
- k2: must use type hints
- k3: PUT supports partial updates (only provided fields are changed)

## Anti-patterns (what counts as failure)
- PUT replacing the entire item instead of merging fields
- missing 404 cases
- no tests
- skipping the receipt marker

## Expected complexity
trivial

## Known trace
trace_id: 8132aad3-c937-4410-8de6-86bf0e0a55ef
manual_label: pass
observations: 45
duration_min: 13.0
produced: tests/test_items_crud.py (6 tests)

## Notes
Built on top of t06's items store. Auto-completed via receipt detection.
Model: deepseek-v4-flash-free (OpenCode default).
