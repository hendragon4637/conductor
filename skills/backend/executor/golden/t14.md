# t14 — Refactor god class

## User intent (verbatim)
Refactor `god_class.py` (a 200-line `OrderProcessor` doing 6 jobs) into 4 small classes with single responsibility. Behavior preserved — include pinning tests on the original.

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- `god_class.py` is refactored into 4 separate classes, each with a single responsibility
- Original `OrderProcessor` public API is preserved as a facade or tests are adapted
- Pinning tests from the original pass unchanged
- Pytest tests exist; `pytest -q` passes

## Constraints (clauses)
- k1: must produce exactly 4 classes with distinct responsibilities
- k2: original behavior must be preserved (all pinning tests pass)
- k3: must include the god_class.py file (original or refactored)

## Anti-patterns (what counts as failure)
- fewer or more than 4 classes
- changing the original public API without adapting tests
- breaking existing tests
- skipping the receipt marker

## Expected complexity
medium

## Known good trace
trace_id:
manual_label:
observations:
duration:

## Notes
None yet.
