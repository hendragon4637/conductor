# t09 — request timing middleware

## User intent (verbatim)
Add a middleware that logs the request method, path, and duration in milliseconds for every request. Use Python logging, log at INFO level. Include a test that verifies the middleware fires (check log output with caplog).

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- Middleware logs every request at INFO level
- Log message includes method, path, and duration_ms fields
- Log format matches `method=%(method)s path=%(path)s duration_ms=%(duration).1f`
- Test uses caplog to capture and assert log output
- Multiple requests in test produce multiple log lines
- `pytest -q` passes

## Constraints (clauses)
- k1: must use Python logging (not print)
- k2: must include a test that captures log output with caplog
- k3: duration must be in milliseconds (float with 1 decimal)

## Anti-patterns (what counts as failure)
- using print instead of logging
- no test for log output
- middleware not firing for all routes
- skipping the receipt marker

## Expected complexity
moderate

## Known trace
trace_id: db76d037-70d2-404c-a3ea-eae276604c9e
manual_label: pass
observations: 37
duration_min: 8.3
produced: tests/test_middleware.py (2 tests)

## Notes
Auto-completed via receipt detection.
Model: deepseek-v4-flash-free (OpenCode default).
