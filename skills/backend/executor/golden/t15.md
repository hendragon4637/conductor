# t15 — Optimize O(n²) to O(n log n) or O(n)

## User intent (verbatim)
Optimize `find_duplicates.py` (currently O(n²) nested-loop) to O(n log n) or O(n). Tests verify the result is identical on 10k random inputs.

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- `find_duplicates.py` contains an optimized version (O(n log n) or O(n))
- Public signature unchanged
- Returns identical results to the original on 10k random inputs
- Pytest test exists that validates correctness on random inputs; `pytest -q` passes

## Constraints (clauses)
- k1: must not change the public function signature
- k2: must include test with 10k random inputs
- k3: must use O(n log n) or O(n) algorithm

## Anti-patterns (what counts as failure)
- changing the public signature
- no performance test
- using nested loops (still O(n²))
- skipping the receipt marker

## Expected complexity
medium

## Known good trace
trace_id:
manual_label:
observations:
duration:

## Notes
None yet.
