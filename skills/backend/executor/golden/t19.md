# t19 — Security audit + patch

## User intent (verbatim)
Audit `vulnerable.py` (provided: contains string-concat SQL, plaintext password log, eval() on user input). Patch all three. Include `test_audit.py` that asserts each vulnerability is closed.

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- Patches three vulnerabilities: SQL injection (string concat → parameterized query), plaintext password in logs (redact), eval() on user input (remove or replace with safe alternative)
- `test_audit.py` contains tests that prove each vulnerability is no longer exploitable
- Pytest tests exist; `pytest -q` passes

## Constraints (clauses)
- k1: must patch all 3 vulnerabilities
- k2: must include a test for each patched vulnerability
- k3: must not introduce new vulnerabilities

## Anti-patterns (what counts as failure)
- missing patch for any of the 3 vulnerabilities
- no test proving the fix
- using eval() or exec() anywhere in the solution
- skipping the receipt marker

## Expected complexity
medium

## Known good trace
trace_id:
manual_label:
observations:
duration:

## Notes
None yet.
