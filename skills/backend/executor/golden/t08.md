# t08 — items search

## User intent (verbatim)
Add GET /items?q=<search> endpoint that filters items by name substring (case-insensitive). Returns 200 with matching items (empty list if none). Include pytest tests.

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- GET /items?q=foo returns items whose name contains "foo" (case-insensitive)
- GET /items?q= returns all items
- GET /items (no q param) returns all items
- Returns 200 with an array (empty array if no match)
- pytest tests exist using TestClient; `pytest -q` passes

## Constraints (clauses)
- k1: must include tests for substring match, case-insensitivity, no match, and empty q
- k2: must use type hints

## Anti-patterns (what counts as failure)
- exact-match-only instead of substring
- case-sensitive filtering
- returning non-array for no matches
- skipping the receipt marker

## Expected complexity
trivial

## Known trace
trace_id: 21ee1093-9999-4225-9cb4-1da66926652b
manual_label: pass
observations: 37
duration_min: 10.8
produced: tests/test_items_search.py (5 tests)

## Notes
Built on top of t06's items store. Auto-completed via receipt detection.
Model: deepseek-v4-flash-free (OpenCode default).
