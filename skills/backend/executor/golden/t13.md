# t13 — Producer-consumer with asyncio.Queue

## User intent (verbatim)
Implement producer-consumer in `pc_queue.py` with `asyncio.Queue` — 2 producers, 3 consumers, bounded queue size, graceful shutdown via `None` sentinel. Test that all items are processed exactly once.

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- `pc_queue.py` contains a producer-consumer setup with `asyncio.Queue`
- 2 producers that generate items
- 3 consumers that process items
- Bounded queue size
- Graceful shutdown via `None` sentinel (consumers stop when receiving sentinel)
- Test verifies all items are processed exactly once
- Pytest test exists using `pytest-asyncio`; `pytest -q` passes

## Constraints (clauses)
- k1: must use asyncio.Queue
- k2: must use None sentinel for shutdown
- k3: must have exactly 2 producers and 3 consumers
- k4: must test that no items are lost or duplicated

## Anti-patterns (what counts as failure)
- using threading instead of asyncio
- missing sentinel-based shutdown
- lost or duplicate items
- skipping the receipt marker

## Expected complexity
medium

## Known good trace
trace_id:
manual_label:
observations:
duration:

## Notes
None yet.
