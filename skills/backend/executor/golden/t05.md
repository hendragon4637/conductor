# t05 — error handling

## User intent (verbatim)
Add GET /divide?a=&b= returning a/b as JSON. Handle division by zero by returning 400 with {"error":"div_by_zero"}. Test both happy path and zero division.

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- GET /divide?a=10&b=2 returns 200 with {"result": 5.0}
- GET /divide?a=10&b=0 returns 400 with {"error": "div_by_zero"}
- Missing params return 422
- Non-numeric params return 422
- Tests for happy path and zero division; `pytest -q` passes

## Constraints (clauses)
- k1: must handle division by zero gracefully (400, not 500)
- k2: must use type hints
- k3: must test both happy path and error case

## Anti-patterns (what counts as failure)
- 500 error on division by zero
- missing input validation
- no test for zero division
- returning non-JSON error response

## Expected complexity
trivial

## Known good trace
trace_id: da2319e1-ee94-4142-b100-3685525d0eeb
manual_label: pass
observations: 62
