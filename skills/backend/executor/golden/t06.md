# t06 — CRUD items (POST + GET)

## User intent (verbatim)
Add POST /items and GET /items/{id} endpoints with an in-memory store. Include pytest tests using FastAPI TestClient.

## Domain
backend

## Intent type
implement

## Success criteria (deterministic)
- POST /items accepts `{"name": str, "price": float}` and returns 201 with the created item (including auto-generated sequential id)
- GET /items/{id} returns 200 with the item JSON
- GET /items/{id} returns 404 for non-existent id
- In-memory store (dict) scoped at module level
- Sequential integer IDs auto-assigned (1, 2, 3, …)
- pytest tests exist using TestClient; `pytest -q` passes

## Constraints (clauses)
- k1: must include tests for both endpoints
- k2: must use type hints
- k3: item must auto-generate a sequential id on creation

## Anti-patterns (what counts as failure)
- using a database instead of in-memory store
- missing 404 case for missing item
- no test for the POST endpoint
- skipping the receipt marker

## Expected complexity
trivial

## Known trace
trace_id: 84438854-4469-4419-bf7b-3df99e819418
manual_label: pass
observations: 37
duration_min: 15.4
produced: tests/test_items.py (4 tests)

## Notes
Redo of failed t02 (which never wrote any code). Auto-completed via receipt detection.
Model: deepseek-v4-flash-free (OpenCode default).
