# t10 — Dockerfile + docker-compose

## User intent (verbatim)
Add a Dockerfile for the FastAPI app (use python:3.12-slim, install deps, uvicorn on port 8000) and a docker-compose.yml that builds and runs it.

## Domain
ops

## Intent type
implement

## Success criteria (deterministic)
- Dockerfile exists at repo root
- docker-compose.yml exists at repo root
- Dockerfile uses python:3.12-slim base image
- Dockerfile copies requirements.txt and runs pip install
- Dockerfile copies app/ directory
- Dockerfile exposes port 8000
- Dockerfile CMD runs uvicorn on 0.0.0.0:8000
- docker-compose.yml defines a service `api` with build and port mapping 8000:8000

## Constraints (clauses)
- k1: must be two separate files (Dockerfile + docker-compose.yml)
- k2: must use correct python:3.12-slim base
- k3: port must be 8000

## Anti-patterns (what counts as failure)
- single combined file
- wrong base image
- missing CMD or wrong port
- skipping the receipt marker

## Expected complexity
trivial

## Known trace
trace_id: ab1c6b65-98a2-4559-bba0-388b41d545e9
manual_label: pass
observations: 19
duration_min: 5.1
produced: Dockerfile, docker-compose.yml

## Notes
Ops-domain task (non-backend). Auto-completed via receipt detection.
Model: deepseek-v4-flash-free (OpenCode default).
