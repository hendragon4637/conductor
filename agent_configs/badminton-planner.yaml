# /opt/aipc/conductor/agent_configs/badminton-planner.yaml
# Planner: decomposes badminton-tracker intent into a plan DAG for executor + reviewer.

agent_config_id: badminton-planner
cli: opencode
harness: opencode
harness_capabilities:
  interactive: true
  headless: true
  session_persistence: true
  sub_agents: true
  tool_calling: native
  data_format: sqlite
  data_path_template: ~/.local/share/opencode/opencode.db
domain: badminton
role: planner
pattern: standalone
version: 1

input_spec_schema: planning_task
output_spec_schema: plan_dag

system_prompt: |
  You are a planning specialist for full-stack CRUD applications.

  You receive a high-level task and decompose it into a sequence of
  plan nodes. Each node specifies: agent_config (e.g. badminton-executor
  or badminton-reviewer), clear instructions, and dependency ordering.

  Hard rules:
  - Output a PlanDAG JSON with nodes and edges.
  - Each node must reference an existing agent_config.
  - Dependencies must be acyclic (planner → executor → reviewer).
  - Do NOT implement anything — only produce the plan.

  After completing the plan, print a JSON object on its own line prefixed
  with `__PLAN_DAG__:`.

allowed_tools:
  - read

permission_policy:
  mode: auto_approve

model_preference: nvidia/gpt-oss-120b

routing_rules:
  pattern_hint: standalone
  on_success:
    - condition: ""
      next_config: null
      terminates_task: true
  on_failure:
    - condition: ""
      next_config: null
      terminates_task: false
      escalate: human_review
  max_iterations: 1
  terminates_task_if_none: true

active: true
