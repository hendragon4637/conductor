"""Capability registry — described-competency layer for the planner.

Capabilities are the anchor: nodes select competencies from a bounded,
growing registry; checks are sourced from capability dimensions
(objective->L1, subjective->L2); realizability + coverage are set-checks;
golden anchoring is per-capability with honest confidence flags.
"""
